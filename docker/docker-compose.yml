services:
  haxall:
    stdin_open: true
    tty: true
    image: phable_haxall_cli
    container_name: phable_haxall_cli_run
    build:
      context: ../../
      dockerfile: phable/docker/Dockerfile
    volumes:
      - ./src/xeto:/app/phable-test/src/xeto
      - ../../xeto:/app/xeto
